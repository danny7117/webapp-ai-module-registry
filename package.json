{
  "name": "webapp-ai-module-registry",
  "version": "1.0.0",
  "description": "WebApp AI 智慧模組庫（manifests + tools）",
  "type": "module",
  "scripts": {
    "start": "node -e \"console.log('no server here')\"",
    "validate": "node scripts/validate_modules.cjs",
    "gen": "node scripts/generate_manifests.cjs registry/registry.ndjson",
    "audit": "node scripts/audit_registry.cjs registry/registry.ndjson"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
